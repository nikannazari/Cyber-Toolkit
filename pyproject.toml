[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cybertoolkit"
version = "0.0.0"
description = "A modular security toolkit framework with Streamlit and FastAPI."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]